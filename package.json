{
  "name": "bludrop-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Static landing page for BluDrop, ready to deploy on Vercel.",
  "scripts": {
    "dev": "npx serve .",
    "start": "npx serve ."
  },
  "dependencies": {
    "resend": "^4.0.0"
  }
}

